-r requirements.txt

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-html>=4.1.0
pytest-cov>=7.0.0
httpx>=0.27.0

# Formatting & linting
# Pinned exactly (not >=) so local, pre-commit, and CI all format identically —
# a floating version here previously drifted ahead of the pre-commit hook's
# pinned rev and caused CI-only formatting failures.
ruff==0.15.20

# Git hooks
pre-commit>=3.7.0
