[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = --html=reports/test-report.html --self-contained-html -m "not integration"
markers =
    integration: tests that hit real network and APIs (deselected by default)
